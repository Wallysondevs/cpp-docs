# std::experimental::reflect::Named

Definido no cabeçalho `[<experimental/reflect>](<#/doc/header/experimental/reflect>)`

```c
template< class T >
concept Named = Object<T> && /* see below */;
```

O `concept` `Named` é satisfeito se e somente se T possui um nome associado (possivelmente vazio).

### Exemplo

| Esta seção está incompleta
Razão: exemplos

### Veja também

| Esta seção está incompleta
Razão: templatização