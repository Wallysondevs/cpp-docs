# std::ranges::chunk_view&lt;V&gt;::iterator&lt;Const&gt;::base

```cpp
constexpr ranges::iterator_t<Base> base() const;  // (desde C++23)
```

  
Retorna o iterator subjacente para o início do chunk atual.

Equivalente a return [` _current__`](<#/doc/ranges/chunk_view/iterator>) ;.

### Parâmetros

(nenhum)

### Valor de retorno

Um iterator.

### Exemplo

| Esta seção está incompleta  
Razão: nenhum exemplo   