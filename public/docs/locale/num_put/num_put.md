# std::num_put&lt;CharT,OutputIt&gt;::num_put

Definido no cabeçalho `[<locale>](<#/doc/header/locale>)`

```c
explicit num_put( std::size_t refs = 0 );
```

  
Cria um [std::num_put](<#/doc/locale/num_put>) facet e encaminha a contagem de referência inicial refs para o construtor da classe base, [`locale::facet::facet()`](<#/>). 

### Parâmetros

refs  |  \-  |  contagem de referência inicial   