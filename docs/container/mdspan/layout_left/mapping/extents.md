# std::layout_left::mapping&lt;Extents&gt;::extents

```cpp
constexpr const extents_type& extents() const noexcept;  // (desde C++23)
```

  
Retorna uma referência const ao objeto extents. Equivalente a `return extents_;`. 

### Parâmetros

(nenhum) 

### Valor de retorno

Uma referência const ao extents. 

### Exemplo

| Esta seção está incompleta  
Razão: nenhum exemplo   
  
### Veja também

[ extents](<#/doc/container/mdspan/extents_mfun>) | obtém o objeto extents   
(função membro pública de `std::mdspan<T,Extents,LayoutPolicy,AccessorPolicy>`)  