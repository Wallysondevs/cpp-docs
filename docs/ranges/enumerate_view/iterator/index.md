# std::ranges::enumerate_view&lt;V&gt;::iterator&lt;Const&gt;::index

```cpp
constexpr difference_type index() const noexcept;  // (desde C++23)
```

  
Retorna o índice subjacente [`_pos__`](<#/doc/ranges/enumerate_view/iterator>) do elemento atual. Equivalente a: `return pos_;`. 

### Parâmetros

(nenhum) 

### Exemplo

| Esta seção está incompleta  
Razão: nenhum exemplo   