# std::packaged_task&lt;R(Args...)&gt;::valid

```cpp
bool valid() const noexcept;  // (desde C++11)
```

  
Verifica se *this possui um estado compartilhado.

### Parâmetros

(nenhum)

### Valor de retorno

`true` se *this possui um estado compartilhado, `false` caso contrário.