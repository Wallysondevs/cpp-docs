# std::experimental::scope_exit&lt;EF&gt;::~scope_exit

~scope_exit() noexcept; |  |  (library fundamentals TS v3)  

  
Chama a função de saída se o `scope_exit` estiver ativo, então destrói o `EF` armazenado (se for um objeto de função) e quaisquer outros membros de dados não estáticos.

### Veja também

[ release](<#/doc/experimental/scope_exit/release>) |  torna o `scope_exit` inativo   
(public member function)  