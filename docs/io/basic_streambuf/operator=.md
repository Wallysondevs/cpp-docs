# std::basic_streambuf&lt;CharT,Traits&gt;::operator=

```cpp
basic_streambuf& operator=( const basic_streambuf& other );  // (desde C++11)
```

  
Atribui os membros de dados de `other` a `*this`.

### Parâmetros

other  |  \-  |  buffer de stream a ser atribuído   
  
### Valor de retorno

`*this`

### Exceções

Pode lançar exceções definidas pela implementação.

### Exemplo

| Esta seção está incompleta  
Razão: nenhum exemplo   