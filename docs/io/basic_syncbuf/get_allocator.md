# std::basic_syncbuf&lt;CharT,Traits,Allocator&gt;::get_allocator

allocator_type get_allocator() const noexcept;

  
Retorna uma cópia do alocador que foi passado como argumento do construtor ou substituído através do operador de atribuição.

### Parâmetros

(nenhum)

### Exemplo

| Esta seção está incompleta  
Razão: sem exemplo   
  
### Veja também

[ (constructor)](<#/doc/io/basic_syncbuf/basic_syncbuf>) |  constrói um objeto `basic_syncbuf`   
(função membro pública)  
[ operator=](<#/>) |  atribui um objeto `basic_syncbuf`   
(função membro pública)