# std::experimental::reflect::Record

Definido no cabeçalho `[<experimental/reflect>](<#/doc/header/experimental/reflect>)`

```c
template< class T >
concept Record = Type<T> && Scope<T> && /* see below */;
```

O conceito `Record` é satisfeito se e somente se T reflete um tipo de classe.

### Exemplo

| Esta seção está incompleta
Razão: exemplos

### Veja também

| Esta seção está incompleta
Razão: templatização