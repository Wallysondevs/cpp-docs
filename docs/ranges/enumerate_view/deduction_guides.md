# guias de dedução para std::ranges::enumerate_view

Definido no cabeçalho `[<ranges>](<#/doc/header/ranges>)`

```c
template< class R >
enumerate_view( R&& ) -> enumerate_view<views::all_t<R>>;
```

O [guia de dedução](<#/doc/language/ctad>) é fornecido para std::ranges::enumerate_view para permitir a dedução a partir de um [`range`](<#/doc/ranges/range>).

### Exemplo

| Esta seção está incompleta
Motivo: sem exemplo