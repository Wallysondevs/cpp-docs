# std::experimental::pmr::polymorphic_allocator&lt;T&gt;::resource

memory_resource* resource() const; |  |  (library fundamentals TS)  

  
Retorna o ponteiro para o recurso de memória usado por este alocador polimórfico.

### Parâmetros

(nenhum)

### Valor de retorno

O ponteiro para o recurso de memória usado por este alocador polimórfico.