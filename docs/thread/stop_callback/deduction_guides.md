# Guias de dedução para std::stop_callback

Definido no cabeçalho `[<stop_token>](<#/doc/header/stop_token>)`

```c
template< class Callback >
stop_callback( std::stop_token, Callback ) -> stop_callback<Callback>;
```

Um [guia de dedução](<#/doc/language/ctad>) é fornecido para [std::stop_callback](<#/doc/thread/stop_callback>) para permitir a dedução a partir de argumentos de tipos invocáveis.

### Exemplo

| Esta seção está incompleta
Razão: nenhum exemplo