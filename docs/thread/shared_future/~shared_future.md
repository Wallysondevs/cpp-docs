# std::shared_future&lt;T&gt;::~shared_future

```cpp
~shared_future();  // (desde C++11)
```

Se *this for o último objeto referenciando o estado compartilhado, destrói o estado compartilhado. Caso contrário, não faz nada.