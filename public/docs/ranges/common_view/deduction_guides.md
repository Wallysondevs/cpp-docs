# Guias de dedução para std::ranges::common_view

Definido no cabeçalho `[<ranges>](<#/doc/header/ranges>)`

```c
template< class R >
common_view( R&& ) -> common_view<views::all_t<R>>;
```

O [guia de dedução](<#/doc/language/ctad>) é fornecido para std::ranges::common_view para permitir a dedução a partir de [`range`](<#/doc/ranges/range>).

### Exemplo

| Esta seção está incompleta
Razão: nenhum exemplo