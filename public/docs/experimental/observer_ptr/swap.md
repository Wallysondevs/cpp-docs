# std::experimental::observer_ptr&lt;W&gt;::swap

constexpr void swap( observer_ptr& other ) noexcept; |  |  (library fundamentals TS v2)  

  
Troca o objeto observado de `*this` e outro objeto `observer_ptr` `other`, invocando `swap` nos ponteiros armazenados de `*this` e `other`.

### Parâmetros

other  |  \-  |  outro objeto `observer_ptr` para trocar o objeto observado   
  
### Valor de retorno

(nenhum) 

### Exemplo

| Esta seção está incompleta  
Razão: nenhum exemplo   