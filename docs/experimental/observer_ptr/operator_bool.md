# std::experimental::observer_ptr&lt;W&gt;::operator bool

constexpr explicit operator bool() const noexcept; |  |  (library fundamentals TS v2)  

  
Verifica se *this possui um objeto observado associado, ou seja, se get() != nullptr. 

### Parâmetros

(nenhum) 

### Valor de retorno

true se *this possui um objeto observado associado, false caso contrário. 

### Exemplo

| Esta seção está incompleta  
Razão: nenhum exemplo   
  
### Veja também

[ get](<#/doc/experimental/observer_ptr/get>) |  retorna um ponteiro para o objeto observado   
(função membro pública)  