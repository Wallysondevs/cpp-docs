# std::raw_storage_iterator&lt;OutputIt,T&gt;::base

OutputIt base() const; | | (desde C++17)

Fornece acesso ao iterador passado no construtor deste raw_storage_iterator.

### Parâmetros

(nenhum)

### Valor de retorno

Um iterador apontando para o mesmo objeto que *this.