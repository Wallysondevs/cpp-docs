# std::pmr::polymorphic_allocator&lt;T&gt;::resource

[std::pmr::memory_resource](<#/doc/memory/memory_resource>)* resource() const; |  |  (desde C++17)  

  
Retorna o ponteiro para o [recurso de memória](<#/doc/memory/memory_resource>) utilizado por este alocador polimórfico.

### Parâmetros

(nenhum)

### Valor de retorno

O ponteiro para o recurso de memória utilizado por este alocador polimórfico.