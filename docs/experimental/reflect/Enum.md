# std::experimental::reflect::Enum

Definido no cabeçalho `[<experimental/reflect>](<#/doc/header/experimental/reflect>)`

```c
template< class T >
concept Enum = Type<T> && Scope<T> && /* see below */;
```

O `concept` `Enum` é satisfeito se e somente se T reflete um tipo de enumeração.

### Exemplo

| Esta seção está incompleta
Razão: exemplos

### Veja também

| Esta seção está incompleta
Razão: templatização