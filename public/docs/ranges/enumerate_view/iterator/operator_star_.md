# std::ranges::enumerate_view&lt;V&gt;::iterator&lt;Const&gt;::operator*

```cpp
constexpr auto operator*() const;  // (desde C++23)
```

Retorna o elemento atual na `enumerate_view`.

Equivalente a: return` `[` _reference-type_`](<#/doc/ranges/enumerate_view/iterator>)([`_pos__`](<#/doc/ranges/enumerate_view/iterator>) , *[`_current__`](<#/doc/ranges/enumerate_view/iterator>));.

### Parâmetros

(nenhum)

### Valor de retorno

O elemento atual.

### Exemplo

| Esta seção está incompleta
Razão: nenhum exemplo

### Veja também

[ operator[]](<#/doc/ranges/enumerate_view/iterator/operator_at>)(C++23) | acessa um elemento por índice
(função membro pública)