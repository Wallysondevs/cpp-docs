# std::error_condition::clear

```cpp
void clear() noexcept;  // (desde C++11)
```

Limpa o estado da condição de erro. Define o valor de erro como ​0​ e a categoria de erro como [std::generic_category](<#/doc/error/generic_category>).

### Parâmetros

(nenhum)

### Valor de retorno

(nenhum)