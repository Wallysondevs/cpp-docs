# std::experimental::pmr::unsynchronized_pool_resource::upstream_resource

memory_resource* upstream_resource() const; |  |  (library fundamentals TS)  

  
Retorna um ponteiro para o recurso de memória upstream. Este é o mesmo valor que o argumento `upstream` passado para o construtor deste objeto. 

### Veja também 

[ (construtor)](<#/doc/experimental/unsynchronized_pool_resource/unsynchronized_pool_resource>) | constrói um `unsynchronized_pool_resource`   
(função membro pública)  