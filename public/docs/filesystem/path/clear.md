# std::filesystem::path::clear

```cpp
void clear() noexcept;  // (desde C++17)
```

Limpa o pathname armazenado. [`empty()`](<#/doc/filesystem/path/empty>) é verdadeiro após a chamada.

### Parâmetros

(nenhum)

### Valor de retorno

(nenhum)