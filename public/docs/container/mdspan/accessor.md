# std::mdspan&lt;T,Extents,LayoutPolicy,AccessorPolicy&gt;::accessor

```cpp
constexpr const accessor_type& accessor() const noexcept;  // (desde C++23)
```

  
Retorna uma referência `const` para o `accessor` [`_acc__`](<#/doc/container/mdspan>). Equivalente a `return acc_;`. 

### Parâmetros

(nenhum) 

### Valor de retorno

Ver acima. 

### Exemplo

| Esta seção está incompleta  
Razão: nenhum exemplo   
  
### Veja também

| Esta seção está incompleta   