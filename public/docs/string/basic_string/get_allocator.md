# std::basic_string&lt;CharT,Traits,Allocator&gt;::get_allocator

allocator_type get_allocator() const; | | (noexcept desde C++20)
(constexpr desde C++20)

Retorna o allocator associado à string.

### Parâmetros

(nenhum)

### Valor de retorno

O allocator associado.

### Complexidade

Constante.

### Veja também

[ allocator](<#/doc/memory/allocator>) | o allocator padrão
(modelo de classe)
[ allocator_traits](<#/doc/memory/allocator_traits>)(C++11) | fornece informações sobre tipos de allocator
(modelo de classe)
[ uses_allocator](<#/doc/memory/uses_allocator>)(C++11) | verifica se o tipo especificado suporta construção uses-allocator
(modelo de classe)