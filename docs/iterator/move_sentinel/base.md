# std::move_sentinel&lt;S&gt;::base

```cpp
constexpr S base() const;  // (desde C++20)
```

  
Retorna o sentinel base subjacente. 

### Parâmetros

(nenhum) 

### Valor de retorno

O sentinel subjacente. 

### Exceções

Pode lançar exceções definidas pela implementação. 

### Exemplo

| Esta seção está incompleta  
Razão: nenhum exemplo   
  
### Veja também

[ base](<#/doc/iterator/move_iterator/base>)(C++11) | acessa o iterator subjacente   
(função membro pública de `std::move_iterator<Iter>`)  