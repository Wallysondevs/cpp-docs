# std::filesystem::recursive_directory_iterator::operator=

```cpp
recursive_directory_iterator& operator=( const recursive_directory_iterator& other ) = default;  // (desde C++17)
recursive_directory_iterator& operator=( recursive_directory_iterator&& other ) = default;  // (desde C++17)
```

  
Atribui um iterador de diretório recursivo. 

### Parâmetros

other  |  \-  |  outro iterador de diretório para atribuir   
  
### Valor de retorno

*this

### Exceções

Pode lançar exceções definidas pela implementação. 

| Esta seção está incompleta   