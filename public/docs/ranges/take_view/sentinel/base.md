# std::ranges::take_view&lt;V&gt;::sentinel&lt;Const&gt;::base

```cpp
constexpr ranges::sentinel_t<Base> base() const;  // (desde C++20)
```

  
Retorna o sentinel subjacente.

### Parâmetros

(nenhum)

### Valor de retorno

uma cópia do sentinel subjacente

### Exemplo

| Esta seção está incompleta  
Razão: nenhum exemplo   