# std::moneypunct&lt;CharT,International&gt;::moneypunct

Definido no cabeçalho `[<locale>](<#/doc/header/locale>)`

```c
explicit moneypunct( std::size_t refs = 0 );
```

  
Cria um facet `[std::moneypunct](<#/doc/locale/moneypunct>)` e encaminha a contagem de referência inicial `refs` para o construtor da classe base, [`locale::facet::facet()`](<#/>). 

### Parâmetros

refs  |  \-  |  contagem de referência inicial   