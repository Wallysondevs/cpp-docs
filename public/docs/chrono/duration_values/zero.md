# std::chrono::duration_values&lt;Rep&gt;::zero

```cpp
static constexpr Rep zero();  // (desde C++11)
(ate C++20)
static constexpr Rep zero() noexcept;  // (desde C++20)
```

  
Retorna uma representação de comprimento zero.

### Parâmetros

(nenhum)

### Valor de retorno

Rep(0)

### Veja também

[ zero](<#/doc/chrono/duration/zero>)[static] | retorna o valor de duração especial zero   
(função membro estática pública de `std::chrono::duration<Rep,Period>`)  
[ min](<#/doc/chrono/duration_values/min>)[static] | retorna a menor representação possível   
(função membro estática pública)  
[ max](<#/doc/chrono/duration_values/max>)[static] | retorna a maior representação possível   
(função membro estática pública)