# std::experimental::unique_resource&lt;R, D&gt;::get_deleter

const D& get_deleter() const noexcept; |  |  (library fundamentals TS v3)  

  
Acessa o objeto deleter que seria usado para descartar o recurso gerenciado. 

### Parâmetros

(nenhum) 

### Valor de retorno

O deleter armazenado. 

### Exemplo

| Esta seção está incompleta  
Razão: nenhum exemplo   
  
### Veja também

[ get_deleter](<#/doc/memory/unique_ptr/get_deleter>) |  retorna o deleter que é usado para a destruição do objeto gerenciado   
(função membro pública de `std::unique_ptr<T,Deleter>`)  