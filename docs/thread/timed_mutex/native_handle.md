# std::timed_mutex::native_handle

```cpp
native_handle_type native_handle();  // (desde C++11)
(nem sempre presente)
```

Retorna o objeto handle nativo subjacente definido pela implementação.

### Parâmetros

(nenhum)

### Valor de retorno

Objeto handle nativo definido pela implementação.

### Exceções

Definido pela implementação.

### Exemplo

| Esta seção está incompleta
Razão: nenhum exemplo