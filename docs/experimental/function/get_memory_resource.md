# std::experimental::function&lt;R(Args...)&gt;::get_memory_resource

[std::experimental::pmr::memory_resource](<#/doc/experimental/memory_resource>)* get_memory_resource() const noexcept; |  |  (library fundamentals TS)   
(removed in library fundamentals TS v3)  

  
Retorna um ponteiro para o recurso de memória usado por este objeto para alocar memória. 

### Parâmetros

(nenhum) 

### Valor de retorno

Um ponteiro para o recurso de memória usado por este objeto para alocar memória. 

### Ver também 

[ (constructor)](<#/doc/experimental/function/function>) |  constrói uma nova instância de `std::experimental::function`   
(função membro pública)  
[ get_allocator](<#/doc/experimental/function/get_allocator>)(library fundamentals TS v3) |  recupera um `std::pmr::polymorphic_allocator` com tipo apagado (type-erased) usado por este objeto para alocar memória   
(função membro pública)