# std::basic_stringbuf&lt;CharT,Traits,Allocator&gt;::get_allocator

```cpp
allocator_type get_allocator() const noexcept;  // (desde C++20)
```

  
Retorna o allocator associado ao container de sequência interno. 

### Parâmetros

(nenhum) 

### Valor de retorno

o allocator associado 

### Veja também

[ get_allocator](<#/doc/string/basic_string/get_allocator>) |  retorna o allocator associado   
(função membro pública de `std::basic_string<CharT,Traits,Allocator>`)  