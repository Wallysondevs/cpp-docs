# std::experimental::latch::is_ready

bool is_ready() const noexcept; | (1) | (concurrency TS)

Retorna true se o contador interno for igual a zero.

### Parâmetros

(nenhum)

### Valor de retorno

true se o contador interno deste latch for igual a zero.