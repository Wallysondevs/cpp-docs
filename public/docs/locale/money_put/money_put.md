# std::money_put&lt;CharT,OutputIt&gt;::money_put

Definido no cabeçalho `[<locale>](<#/doc/header/locale>)`

```c
explicit money_put( std::size_t refs = 0 );
```

  
Cria um facet [std::money_put](<#/doc/locale/money_put>) e encaminha a contagem de referência inicial `refs` para o construtor da classe base, [`locale::facet::facet()`](<#/>). 

### Parâmetros

refs  |  \-  |  contagem de referência inicial   