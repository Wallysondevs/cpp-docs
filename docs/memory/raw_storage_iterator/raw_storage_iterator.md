# std::raw_storage_iterator&lt;OutputIt,T&gt;::raw_storage_iterator

explicit raw_storage_iterator( OutputIt it );

  
Inicializa o iterator para apontar para o mesmo valor para o qual `it` aponta.

### Parâmetros

it  |  \-  |  local para o qual apontar   