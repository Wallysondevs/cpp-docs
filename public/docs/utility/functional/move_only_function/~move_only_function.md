# std::move_only_function::~move_only_function

```cpp
~move_only_function();  // (desde C++23)
```

  
Destrói o objeto `std::move_only_function`. Se o `std::move_only_function` não estiver vazio, seu alvo também é destruído. 

### Veja também

[ (destrutor)](<#/doc/utility/functional/function/~function>) |  destrói uma instância de `std::function`   
(função membro pública de `std::function<R(Args...)>`)  