# std::num_get&lt;CharT,InputIt&gt;::num_get

Definido no cabeçalho `[<locale>](<#/doc/header/locale>)`

```c
explicit num_get( std::size_t refs = 0 );
```

  
Cria um facet [std::num_get](<#/doc/locale/num_get>) e encaminha a contagem de referência inicial refs para o construtor da classe base, [`locale::facet::facet()`](<#/>). 

### Parâmetros

refs  |  \-  |  contagem de referência inicial   