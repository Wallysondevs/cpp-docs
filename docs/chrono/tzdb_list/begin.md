# std::chrono::tzdb_list::begin

```cpp
const_iterator begin() const noexcept;  // (desde C++20)
const_iterator cbegin() const noexcept;  // (desde C++20)
```

  
Retorna um iterator que se refere ao primeiro std::chrono::tzdb no container. 

### Valor de retorno

Um iterator que se refere ao primeiro std::chrono::tzdb no container. 