# std::match_results&lt;BidirIt,Alloc&gt;::~match_results

```cpp
~match_results();  // (desde C++11)
```

  
Destrói o objeto `match_results` e os sub-matches associados. 

### Complexidade

Linear no número de sub-matches. 