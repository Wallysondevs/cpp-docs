# std::basic_string&lt;CharT,Traits,Allocator&gt;::~basic_string

~basic_string(); |  | (constexpr desde C++20)  

  
Destrói o `basic_string`. Os destrutores dos elementos são chamados e o armazenamento utilizado é desalocado. 

### Complexidade

Tipicamente constante (formalmente linear). 