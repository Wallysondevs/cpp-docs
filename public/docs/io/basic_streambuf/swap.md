# std::basic_streambuf&lt;CharT,Traits&gt;::swap

```cpp
void swap( basic_streambuf& other );  // (desde C++11)
```

Troca o conteúdo do stream buffer com o de `other`.

### Parâmetros

- **other** — stream buffer para trocar o conteúdo

### Valor de retorno

(nenhum)

### Exceções

Pode lançar exceções definidas pela implementação.

### Exemplo

| Esta seção está incompleta
Razão: nenhum exemplo