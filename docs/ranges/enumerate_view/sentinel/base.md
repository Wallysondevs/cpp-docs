# std::ranges::enumerate_view&lt;V&gt;::sentinel&lt;Const&gt;::base

```cpp
constexpr ranges::sentinel_t<Base> base() const;  // (desde C++23)
```

  
Retorna o sentinel subjacente. Equivalente a return` `[` _end__`](<#/doc/ranges/enumerate_view/sentinel>) ;. 

### Parâmetros

(nenhum) 

### Valor de retorno

Uma cópia do sentinel subjacente. 

### Exemplo

| Esta seção está incompleta  
Razão: nenhum exemplo   