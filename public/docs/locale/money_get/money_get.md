# std::money_get&lt;CharT,InputIt&gt;::money_get

Definido no cabeçalho `[<locale>](<#/doc/header/locale>)`

```c
explicit money_get( std::size_t refs = 0 );
```

  
Cria um facet `[std::money_get](<#/doc/locale/money_get>)` e encaminha a contagem de referência inicial `refs` para o construtor da classe base, [`locale::facet::facet()`](<#/>). 

### Parâmetros

refs  |  \-  |  contagem de referência inicial   