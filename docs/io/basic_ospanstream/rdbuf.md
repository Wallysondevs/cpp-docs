# std::basic_ospanstream&lt;CharT,Traits&gt;::rdbuf

```cpp
std::basic_spanbuf<CharT, Traits>* rdbuf() const noexcept;  // (desde C++23)
```

Retorna um ponteiro para o objeto de dispositivo bruto encapsulado.

### Parâmetros

(nenhum)

### Valor de retorno

Ponteiro para o objeto de dispositivo bruto encapsulado.

### Exemplo

| Esta seção está incompleta
Razão: nenhum exemplo

### Veja também

[ rdbuf](<#/doc/io/basic_ios/rdbuf>) | gerencia o buffer de stream associado
(função membro pública de `std::basic_ios<CharT,Traits>`)