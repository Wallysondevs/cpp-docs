# std::experimental::reflect::Expression

Definido no cabeçalho `[<experimental/reflect>](<#/doc/header/experimental/reflect>)`

```c
template< class T >
concept Expression = Object<T> && /* see below */;
```

O `concept` `Expression` é satisfeito se e somente se T reflete uma expressão.

### Exemplo

| Esta seção está incompleta
Razão: exemplos

### Veja também

| Esta seção está incompleta
Razão: templatização