# std::regex_iterator&lt;BidirIt,CharT,Traits&gt;::operator=

```cpp
regex_iterator& operator=( const regex_iterator& other );  // (desde C++11)
```

  
Atribui o conteúdo de other.

### Parâmetros

other  |  \-  |  iterador regex para atribuir   
  
### Valor de retorno

*this