# std::experimental::observer_ptr&lt;W&gt;::operator element_type*

constexpr explicit operator element_type*() const noexcept; |  |  (library fundamentals TS v2)  

  
Fornece uma conversão explícita para o tipo do ponteiro armazenado.

### Parâmetros

(nenhum) 

### Valor de retorno

Um ponteiro para o objeto observado, ou seja, get(). 

### Exemplo

| Esta seção está incompleta  
Razão: nenhum exemplo   
  
### Veja também

[ get](<#/doc/experimental/observer_ptr/get>) | retorna um ponteiro para o objeto observado   
(função membro pública)  