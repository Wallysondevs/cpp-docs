# std::mdspan&lt;T,Extents,LayoutPolicy,AccessorPolicy&gt;::mapping

```cpp
constexpr const mapping_type& mapping() const noexcept;  // (desde C++23)
```

  
Retorna o mapeamento de layout. Equivalente a return` `[`_map__`](<#/doc/container/mdspan>) ;. 

### Parâmetros

(nenhum) 

### Valor de retorno

Uma referência const para o objeto de mapeamento de layout subjacente de [`mapping_type`](<#/doc/container/mdspan>). 

### Exemplo

| Esta seção está incompleta  
Razão: nenhum exemplo   
  
### Veja também

| Esta seção está incompleta   