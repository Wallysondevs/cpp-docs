# std::random_device::min

```cpp
static constexpr result_type min();  // (desde C++11)
```

Retorna o valor mínimo potencialmente gerado pelo motor de números aleatórios.

### Valor de retorno

0u

### Complexidade

Constante.

### Veja também

[ max](<#/doc/numeric/random/random_device/max>)[static] | obtém o maior valor possível no intervalo de saída
(função membro estática pública)