# std::regex_token_iterator&lt;BidirIt,CharT,Traits&gt;::operator=

```cpp
regex_token_iterator& operator=( const regex_token_iterator& other );  // (desde C++11)
```

  
Atribui o conteúdo de `other`. 

### Parâmetros

other  |  \-  |  iterator de token regex a ser atribuído   
  
### Valor de retorno

*this