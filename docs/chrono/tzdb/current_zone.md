# std::chrono::tzdb::current_zone

const [std::chrono::time_zone](<#/doc/chrono/time_zone>)* current_zone() const; |  |  (desde C++20)  

  
Obtém um ponteiro para um `std::chrono::time_zone` neste banco de dados que representa o fuso horário local do computador.

### Valor de retorno

Um ponteiro para o `std::chrono::time_zone` neste banco de dados que representa o fuso horário local do computador.