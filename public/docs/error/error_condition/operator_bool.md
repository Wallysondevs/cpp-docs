# std::error_condition::operator bool

```cpp
explicit operator bool() const noexcept;  // (desde C++11)
```

Verifica se o valor de erro armazenado não é zero.

### Parâmetros

(nenhum)

### Valor de retorno

true se value != 0, false caso contrário.